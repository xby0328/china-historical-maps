---
name: china-historical-maps
description: 使用 Python 和本地 RStata 历年底图绘制中国省、市、区县地图，支持南海小地图 mini、长版 long、行政区填色、经纬度散点及栅格叠加。用于中国行政区专题地图、历年地图对比和课程代码迁移。
---

# 中国历年行政区地图

将用户的数据绘成可复现地图；使用 GeoPandas、Matplotlib、Rasterio。课程来源及数据目录配置见 [references/course.md](references/course.md)，首次使用先读该文件。只引用本地课程数据，不将原讲义、字体、数据或完整原代码打包到 Skill。

## 决策顺序

1. 确定行政层级、**数据年份与边界年份**、研究范围、mini/long、变量单位和图型。从用户数据和上下文推断；不明确且影响结论时再问。未指定版式可采用 mini。文件仅到 2021 年；较新数据可配明确标注的固定边界，不能将 2021 底图称作新年份边界。
2. 使用 [scripts/china_map.py](scripts/china_map.py) 的 `inventory` / `inspect` 核对文件、CRS、字段、几何、重复代码。每个要用的年份单独检查。市级保留直辖市，县级使用 `PAC`；图形数不是研究样本数。
3. 校验表格唯一键、单位、缺失、有限数值和匹配结果。多期表先筛年份；按明确口径聚合后再合并。默认缺失保留为空白/灰色，只有“完整事件名录中未出现代表零”得到确认才能补零。代码只规范格式，不凭名称或前缀猜测历史对应关系。
4. 底图 mini 已包含缩放平移的南海图形，不能再次变换。空间连接、距离、面积、栅格裁剪使用真实位置的 long；显示叠加层最后才变换。mini 几何即使带 CRS 也不是可直接分析的真实坐标。
5. 按图型调用脚本，或写项目内普通 Python 脚本导入其辅助函数。不要直接运行课程主程序：其相对路径、年份混用、缺失处理和色条问题见课程笔记。命令示例与扩展方式见 [references/recipes.md](references/recipes.md)。
6. PNG 预览后交付 PNG + PDF/SVG、脚本/配置和匹配审计。检查等比例、中文字体、图例区间/单位、缺失与零的区分、南海叠加位置、线图层、标题来源和研究范围。跨年对比共用色标/分段/画布；逐年边界变化与固定边界比较必须区分。

## 绘图选择

- 连续变量：默认线性 `Normalize`；正值跨度大且解释合理时用 `LogNorm`，零/负值不静默删除或加一。色条和图层共用同一个 `norm`、`cmap`。
- 分段填色：显式断点 + `BoundaryNorm`；自然断点、分位数要报告算法和实际断点，跨年共用一组。原课程名字含 `discrete` 的函数实际是连续对数色标。
- 分类/正负值：项目脚本按含义用离散色或以有意义的中心构造发散色标。不要将正负效应默认套进对数色标。
- 散点：确认 WGS84/GCJ-02/BD-09 来源。辅助脚本接收已确认 CRS 的坐标；投影转换不等于互联网坐标纠偏。逐行输出坏坐标清单，不将有效坐标筛选后的企业数冒充全部企业数。
- 栅格：按变量选择 average（强度）/sum（可加总量）/nearest 或 mode（类别）；预览降采样与统计聚合分开。详细边界、掩膜、nodata、像元中心和内存策略见 recipes。
- 局部省域/城市群图：先在 long 真实几何按经核对的代码筛选，再定范围。不可用 mini 总边界框裁剪局部图；全国底图中非研究地区用背景色，不删除必要地理线条。

## 本地助手

`python <skill目录>/scripts/china_map.py --help`

脚本提供 `inventory`、`inspect`、`base`、`choropleth`、`points`、`raster-points`、`raster-polygons`。通过 `--course-root` 或 `CHINA_MAP_COURSE_ROOT` 指定本地课程路径；未设置时查找当前工作目录的 `course-data`。缺依赖时用实际解释器的 `python -m pip install ...` 安装所需包；不要为此重建已有研究环境。运行过程保留 warnings。

输出写入用户项目的新目录，保留原输入。脚本默认拒绝覆盖已有同名地图；`--overwrite` 仅在用户明确要求覆盖时使用。全国课程底图可用于复现示例，其包含的线条及行政区不能据此宣称已经通过正式出版的地图审查。
